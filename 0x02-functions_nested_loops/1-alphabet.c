#include "main.h"

/**
 * main -  prints the alphabet in lowercase
 * Return: Always 0
 */
int main(void)
{
	char lo;

	for (lo = 'a'; lo <= 'z'; lo++)
		_putchar(lo);

	putchar('\n');

	return (0);
}
